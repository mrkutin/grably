import SwiftUI

/// Grably brand palette, sampled from the app icon / lockup.
enum BrandColor {
    /// Deep navy — icon background and wordmark. #1E2A4A
    static let navy = Color(red: 0x1E / 255, green: 0x2A / 255, blue: 0x4A / 255)
    /// Signature yellow (Pac-Man) — also the app AccentColor. #FFC43D
    static let yellow = Color(red: 0xFF / 255, green: 0xC4 / 255, blue: 0x3D / 255)
    /// Play-button red. #FF0000
    static let red = Color(red: 0xFF / 255, green: 0x00 / 255, blue: 0x00 / 255)
    /// Motto amber. #C9821A
    static let amber = Color(red: 0xC9 / 255, green: 0x82 / 255, blue: 0x1A / 255)
}
